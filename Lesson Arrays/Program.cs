namespace Lesson_Arrays
{
    internal class Program
    {
        static void Main(string[] args)
        {
            string[] FootballClub = new string[5];
                        
            foreach (string name in FootballClub)
            {
                
                Console.WriteLine(name);
            }
            foreach (string name in FootballClub)
            {
                Console.WriteLine(name);
            }
        }
    }
}